# Bullmet — публичная часть, большой UI/UX этап

Что исправлено:

- Навигация в хедере: пункт «Производство» ведет на отдельную страницу `/production`.
- Кнопка «Заказать расчет» ведет на рабочий блок заявки `/services#request`.
- Плитки направлений на главной больше не ведут все в один и тот же каталог: у каждой свой URL/фильтр/раздел.
- Популярные товары на главной визуально приведены к логике карточек каталога.
- Каталог: фильтр переработан в более удобный современный блок.
- Каталог: карточка открывается кликом по любой области, кнопка корзины работает отдельно.
- Каталог: убрана кнопка «Подробнее».
- Каталог: цена и корзина выровнены в одну линию.
- Поиск переработан: нормальная модалка, быстрые запросы, аккуратные результаты.
- Мобильный хедер переработан: бургер-меню открывается отдельной панелью.
- Добавлена нижняя мобильная панель быстрого перехода: Главная, Каталог, Расчет, Корзина, Профиль.
- Страницы `/services`, `/about`, `/contacts` переработаны визуально.
- Добавлена страница `/production`.
- Вход и регистрация визуально разделены и больше не выглядят одинаково.

Проверка:

- `npx tsc --noEmit` проходит без ошибок.
- `next build` доходит до `Compiled successfully`, затем в контейнере упирается во время ожидания на этапе `Collecting page data`.
